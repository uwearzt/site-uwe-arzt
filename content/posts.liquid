---
title: All Blog posts
layout: default.liquid
---
<div>
  {% assign last_post_date = "" %}
  {% assign in_list = false %}
  {% for post in collections.posts.pages %}
    <!-- generate year of posts -->
    {% assign post_date = post.published_date | date: "%Y" %}

    {% if post_date != last_post_date %}
      {% assign last_post_date = post_date %}

      <!-- end old list -->
      {% if in_list == true %}
        {% assign in_list = false %}
        </ul>
      {% endif %}
        
      <h3>{{ post_date }}</h3>

      <!-- start new list -->
      {% if in_list == false %}
        {% assign in_list = true %}
        <ul id="list">
      {% endif %}

    {% endif %}     
    <li>
       <a href="{{ post.permalink }}">{{ post.title }}</a>
       <div class="meta">{{ post.published_date | date: "%d.%m.%Y" }}</div>
    </li>
  {% endfor %}
</div>
