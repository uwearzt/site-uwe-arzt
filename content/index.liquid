extends: default.liquid

title: Actual posts
shortlink: 42
---

<div >
    {% for post in posts limit:5 %}
     	<div class="blog-post">
     		<h1 class="blog-post-title">{{ post.title }}</h1>
     		<article class="post">
     			{{ post.excerpt }}
     			<footer>
        			<a href={{ post.path }}>{{ post.date | date: "%d.%m.%Y" }} | Read more →</a>
   				 </footer>
			</article>
    	</div>
    {% endfor %}
</div>
